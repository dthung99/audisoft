{
  "name": "audisoft-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "yarn dev:backend & yarn dev:frontend",
    "dev:frontend": "yarn --cwd fe dev",
    "dev:backend": "cd be && mvn spring-boot:run"
  }
}